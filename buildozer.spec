[app]# (str) Título de tu aplicacióntitle = MiAppPydroid# (str) Nombre del paquetepackage.name = miapptest# (str) Dominio del paquete (puedes dejar este)package.domain = org.test# (str) Directorio donde está el main.py (punto significa el mismo lugar)source.dir = .# (list) Extensiones de archivos a incluirsource.include_exts = py,png,jpg,kv,atlas# (list) Requerimientos (AQUÍ PON LAS LIBRERÍAS QUE USAS)# Si usas KivyMD cámbialo a: python3,kivy==2.3.0,kivymd,requestsrequirements = python3,kivy==2.3.0# (str) Orientación (landscape, sensorLandscape, portrait o all)orientation = portrait# (bool) Indicar si la aplicación debe ser a pantalla completafullscreen = 1# (int) Android API a usar (Usa 33 para GitHub Actions)android.api = 33# (int) Mínimo Android SDK permitidoandroid.minapi = 21# (str) Arquitectura de Android (GitHub soporta arm64-v8a perfectamente)android.archs = arm64-v8a# (bool) Aceptar automáticamente las licencias del SDKandroid.accept_sdk_license = True# (str) Formato de la APKandroid.release_artifact = apk[buildozer]# (int) Nivel de log (2 para ver errores detallados)log_level = 2# (str) Rama de python-for-android a usarp4a.branch = master